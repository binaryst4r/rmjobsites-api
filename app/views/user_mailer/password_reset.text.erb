Reset your RMJobsites password

Hi<%= " #{@user.given_name}" if @user.given_name.present? %>,

We received a request to set or reset the password on your RMJobsites account. Use the link below to choose a new password. The link expires in 15 minutes.

<%= @reset_url %>

If you did not request this, you can safely ignore this email.

-- RM Jobsite Solutions
