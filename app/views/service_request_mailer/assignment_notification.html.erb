<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #2563eb;
        color: white;
        padding: 20px;
        text-align: center;
      }
      .content {
        background-color: #f9fafb;
        padding: 20px;
        border: 1px solid #e5e7eb;
      }
      .detail-row {
        margin: 10px 0;
        padding: 10px;
        background-color: white;
        border-radius: 4px;
      }
      .label {
        font-weight: bold;
        color: #4b5563;
      }
      .footer {
        margin-top: 20px;
        padding-top: 20px;
        border-top: 1px solid #e5e7eb;
        font-size: 12px;
        color: #6b7280;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>New Service Request Assignment</h1>
      </div>

      <div class="content">
        <p>Hi <%= @assigned_to_user.given_name || @assigned_to_user.email %>,</p>

        <p><%= @assigned_by_user.given_name || @assigned_by_user.email %> has assigned you a new service request.</p>

        <h2>Service Request Details:</h2>

        <div class="detail-row">
          <span class="label">Customer:</span> <%= @service_request.customer_name %>
        </div>

        <div class="detail-row">
          <span class="label">Company:</span> <%= @service_request.company %>
        </div>

        <div class="detail-row">
          <span class="label">Service Requested:</span> <%= @service_request.service_requested %>
        </div>

        <div class="detail-row">
          <span class="label">Equipment:</span> <%= @service_request.manufacturer %> <%= @service_request.model %>
        </div>

        <div class="detail-row">
          <span class="label">Serial Number:</span> <%= @service_request.serial_number %>
        </div>

        <div class="detail-row">
          <span class="label">Pickup Date:</span> <%= @service_request.pickup_date.strftime('%B %d, %Y') %>
        </div>

        <div class="detail-row">
          <span class="label">Return Date:</span> <%= @service_request.return_date.strftime('%B %d, %Y') %>
        </div>

        <% if @service_request.dropped_or_impacted %>
        <div class="detail-row">
          <span class="label">Note:</span> Equipment was dropped or impacted
        </div>
        <% end %>

        <% if @service_request.needs_rush %>
        <div class="detail-row">
          <span class="label">Priority:</span> Rush service needed
        </div>
        <% end %>

        <p style="margin-top: 20px;">Please review and process this service request at your earliest convenience.</p>
      </div>

      <div class="footer">
        <p>This is an automated notification from RMJobsites.</p>
      </div>
    </div>
  </body>
</html>
